unzip train.csv and test.csv files in the current directory

install all python packages via command (on Ubuntu):
pip install Keras, scikit-learn, seaborn, matplotlib, pandas, imbalanced-learn, jupyter
